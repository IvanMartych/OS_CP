# Структура проекта

```
OS_CP/
│
├── 📄 README.md              # Основная документация проекта
├── 📄 QUICKSTART.md          # Быстрый старт за 3 минуты
├── 📄 EXAMPLES.md            # Примеры использования
├── 📄 ANALYSIS.md            # Детальный анализ проекта
├── 📄 PROJECT_STRUCTURE.md   # Этот файл
│
├── 🔧 Makefile               # Сборка проекта
├── 🔧 run.sh                 # Скрипт для удобного запуска
├── 🔧 .gitignore             # Игнорируемые файлы для Git
│
├── 📚 common.h               # Общие структуры и константы
├── 📚 common.c               # Реализация общих функций
│
├── 🖥️  server.c               # Серверная часть игры
└── 💻 client.c               # Клиентская часть игры
```

---

## Описание модулей

### 📚 `common.h` / `common.c` - Общий модуль

**Назначение:** Общие структуры данных и функции, используемые и сервером, и клиентом.

**Ключевые компоненты:**

#### Константы
```c
#define MAX_GAME_NAME 64      // Максимальная длина имени игры
#define MAX_PLAYER_NAME 32    // Максимальная длина имени игрока
#define MAX_PLAYERS 10        // Максимум игроков в одной игре
#define SECRET_LENGTH 4       // Длина секретного числа
```

#### Перечисления
```c
typedef enum {
    MSG_CREATE_GAME,    // Создать игру
    MSG_JOIN_GAME,      // Присоединиться к игре
    MSG_FIND_GAME,      // Найти доступную игру
    MSG_MAKE_GUESS,     // Сделать попытку
    // ... и другие
} MessageType;
```

#### Структуры
```c
// Структура сообщения между клиентом и сервером
typedef struct {
    MessageType type;
    char game_name[MAX_GAME_NAME];
    char player_name[MAX_PLAYER_NAME];
    int max_players;
    int guess[SECRET_LENGTH];
    GuessResult result;
    // ...
} Message;

// Результат попытки угадывания
typedef struct {
    int bulls;
    int cows;
    int attempt_number;
    char player_name[MAX_PLAYER_NAME];
} GuessResult;
```

#### Функции
```c
void generate_secret(int *secret);
void calculate_bulls_cows(int *secret, int *guess, int *bulls, int *cows);
int is_valid_number(int *number);
int send_message(void *socket, Message *msg);
int recv_message(void *socket, Message *msg);
```

---

### 🖥️ `server.c` - Серверная часть

**Назначение:** Управление играми, координация игроков, обработка запросов.

**Ключевые компоненты:**

#### Структуры данных
```c
// Информация об игроке
typedef struct {
    char name[MAX_PLAYER_NAME];
    int is_active;
    int attempts;
} Player;

// Состояние игры
typedef struct {
    char name[MAX_GAME_NAME];
    int secret[SECRET_LENGTH];
    int max_players;
    int current_players;
    Player players[MAX_PLAYERS];
    int is_active;
    int is_finished;
    char winner[MAX_PLAYER_NAME];
} Game;
```

#### Глобальные переменные
```c
Game games[100];        // Массив всех игр
int game_count = 0;     // Счётчик игр
int running = 1;        // Флаг работы сервера
```

#### Основные функции

**Поиск игр:**
```c
Game* find_game_by_name(const char *name);
Game* find_available_game();
```

**Обработка запросов:**
```c
void handle_create_game(Message *request, Message *response);
void handle_join_game(Message *request, Message *response);
void handle_find_game(Message *request, Message *response);
void handle_make_guess(Message *request, Message *response);
void handle_list_games(Message *request, Message *response);
```

**Главный цикл:**
```c
int main() {
    // Инициализация ZeroMQ
    void *context = zmq_ctx_new();
    void *socket = zmq_socket(context, ZMQ_REP);
    zmq_bind(socket, "tcp://*:5555");
    
    // Цикл обработки запросов
    while (running) {
        recv_message(socket, &request);
        process_message(&request, &response);
        send_message(socket, &response);
    }
    
    // Очистка
    zmq_close(socket);
    zmq_ctx_destroy(context);
}
```

---

### 💻 `client.c` - Клиентская часть

**Назначение:** Пользовательский интерфейс, взаимодействие с сервером.

**Ключевые компоненты:**

#### UI функции
```c
void print_menu();           // Главное меню
void print_game_rules();     // Правила игры
int read_number(int *number); // Чтение числа от пользователя
```

#### Действия пользователя
```c
void create_game(void *socket, const char *player_name);
void join_game(void *socket, const char *player_name);
void find_game(void *socket, const char *player_name);
void list_games(void *socket);
```

#### Игровой процесс
```c
void play_game(void *socket, const char *player_name, const char *game_name) {
    // Цикл попыток угадывания
    while (1) {
        // Ввод числа
        read_number(request.guess);
        
        // Отправка на сервер
        send_message(socket, &request);
        recv_message(socket, &response);
        
        // Обработка результата
        if (response.type == MSG_GAME_WON) {
            // Победа!
            break;
        }
    }
}
```

#### Главный цикл
```c
int main() {
    // Подключение к серверу
    void *context = zmq_ctx_new();
    void *socket = zmq_socket(context, ZMQ_REQ);
    zmq_connect(socket, "tcp://localhost:5555");
    
    // Цикл меню
    while (1) {
        print_menu();
        // Обработка выбора пользователя
        switch (choice) {
            case 1: create_game(socket, player_name); break;
            case 2: join_game(socket, player_name); break;
            // ...
        }
    }
}
```

---

## Поток данных

### 1. Создание игры

```
┌─────────┐                              ┌─────────┐
│ Клиент  │                              │ Сервер  │
└────┬────┘                              └────┬────┘
     │                                        │
     │ MSG_CREATE_GAME                        │
     │  - game_name: "mygame"                 │
     │  - player_name: "Ivan"                 │
     │  - max_players: 2                      │
     ├──────────────────────────────────────► │
     │                                        │
     │                           1. Создать игру
     │                           2. Сгенерировать секрет
     │                           3. Добавить игрока
     │                                        │
     │ MSG_GAME_CREATED                       │
     │  - game_name: "mygame"                 │
     │  - player_count: 1                     │
     │ ◄──────────────────────────────────────┤
     │                                        │
```

### 2. Присоединение к игре

```
┌─────────┐                              ┌─────────┐
│ Клиент  │                              │ Сервер  │
└────┬────┘                              └────┬────┘
     │                                        │
     │ MSG_JOIN_GAME                          │
     │  - game_name: "mygame"                 │
     │  - player_name: "Anna"                 │
     ├──────────────────────────────────────► │
     │                                        │
     │                           1. Найти игру
     │                           2. Проверить места
     │                           3. Добавить игрока
     │                                        │
     │ MSG_JOINED_GAME                        │
     │  - game_name: "mygame"                 │
     │  - player_count: 2                     │
     │ ◄──────────────────────────────────────┤
     │                                        │
```

### 3. Попытка угадывания

```
┌─────────┐                              ┌─────────┐
│ Клиент  │                              │ Сервер  │
└────┬────┘                              └────┬────┘
     │                                        │
     │ MSG_MAKE_GUESS                         │
     │  - game_name: "mygame"                 │
     │  - player_name: "Ivan"                 │
     │  - guess: [1, 2, 3, 4]                 │
     ├──────────────────────────────────────► │
     │                                        │
     │                           1. Найти игру
     │                           2. Проверить игрока
     │                           3. Валидация числа
     │                           4. Подсчёт быков/коров
     │                           5. Проверка победы
     │                                        │
     │ MSG_GUESS_RESULT / MSG_GAME_WON        │
     │  - bulls: 2                            │
     │  - cows: 1                             │
     │  - attempt_number: 5                   │
     │ ◄──────────────────────────────────────┤
     │                                        │
```

---

## Сетевая архитектура

### Протокол ZeroMQ REQ-REP

```
                    ┌─────────────────┐
                    │   Клиент 1      │
                    │   (REQ socket)  │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │   Клиент 2      │
                    │   (REQ socket)  │
                    └────────┬────────┘
                             │
    tcp://localhost:5555     │
    ─────────────────────────┼────────────────
                             │
                    ┌────────▼────────┐
                    │     Сервер      │
                    │   (REP socket)  │
                    │                 │
                    │  ┌───────────┐  │
                    │  │  Game 1   │  │
                    │  ├───────────┤  │
                    │  │  Game 2   │  │
                    │  ├───────────┤  │
                    │  │  Game 3   │  │
                    │  └───────────┘  │
                    └─────────────────┘
```

**Особенности:**
- **Синхронная связь:** Клиент ждёт ответа перед следующим запросом
- **Надёжность:** Гарантированная доставка сообщений
- **Простота:** Автоматическое управление соединениями

---

## Состояния системы

### Жизненный цикл игры

```
┌──────────────┐
│  NOT_CREATED │
└──────┬───────┘
       │ CREATE_GAME
       ▼
┌──────────────┐
│    ACTIVE    │ ◄──┐
│  (waiting)   │    │ JOIN_GAME
└──────┬───────┘    │
       │            │
       ├────────────┘
       │ MAKE_GUESS
       ▼
┌──────────────┐
│    ACTIVE    │
│  (playing)   │
└──────┬───────┘
       │
       │ WIN condition
       ▼
┌──────────────┐
│   FINISHED   │
└──────────────┘
```

### Состояния игрока

```
┌──────────────┐
│ NOT_IN_GAME  │
└──────┬───────┘
       │
       │ CREATE/JOIN/FIND
       ▼
┌──────────────┐
│  IN_GAME     │
│ (is_active)  │
└──────┬───────┘
       │
       │ MAKE_GUESS
       ▼
┌──────────────┐
│  PLAYING     │
│ (attempts++)  │
└──────┬───────┘
       │
       ├──► WIN ───────┐
       │               │
       └──► CONTINUE ──┘
```

---

## Безопасность и валидация

### Проверки на сервере

1. **Валидация игры:**
   - Уникальность имени игры
   - Лимит количества игр (100)
   - Корректное количество игроков (1-10)

2. **Валидация игрока:**
   - Игрок в игре
   - Игра не завершена
   - Уникальность имени в игре

3. **Валидация числа:**
   - Длина = 4
   - Все цифры 0-9
   - Все цифры уникальны

### Обработка ошибок

```c
// Пример валидации
if (!is_valid_number(request->guess)) {
    response->type = MSG_ERROR;
    strcpy(response->error_msg, "Неверный формат числа");
    return;
}
```

---

## Расширение проекта

### Возможные точки расширения

1. **Персистентность:**
   ```c
   void save_game_state(Game *game, FILE *file);
   void load_game_state(Game *game, FILE *file);
   ```

2. **Асинхронность:**
   ```c
   // Использование ROUTER-DEALER
   void *router = zmq_socket(context, ZMQ_ROUTER);
   void *dealer = zmq_socket(context, ZMQ_DEALER);
   zmq_proxy(router, dealer, NULL);
   ```

3. **Статистика:**
   ```c
   typedef struct {
       int total_games_played;
       int total_wins;
       float average_attempts;
       time_t last_played;
   } PlayerStats;
   ```

4. **Рейтинг:**
   ```c
   int calculate_rating(int attempts, int max_players) {
       return 1000 + (10 - attempts) * 100 - max_players * 10;
   }
   ```

---

## Производительность

### Метрики

- **Размер сообщения:** ~400 байт
- **Задержка:** 1-2 мс (localhost)
- **Память сервера:** ~2-3 МБ
- **CPU:** <5% при активной игре

### Ограничения

- Максимум 100 игр (программное ограничение)
- Последовательная обработка (REQ-REP паттерн)
- Нет персистентности (всё в памяти)

---

## Зависимости

### Обязательные
- `gcc` - компилятор C
- `make` - утилита сборки
- `libzmq3-dev` - библиотека ZeroMQ

### Системные библиотеки
- `stdio.h` - ввод-вывод
- `stdlib.h` - стандартные функции
- `string.h` - работа со строками
- `time.h` - время (для random)
- `signal.h` - обработка сигналов (только сервер)
- `unistd.h` - POSIX API (только сервер)

---

## Тестирование

### Юнит-тесты (потенциальные)
```c
void test_generate_secret();
void test_calculate_bulls_cows();
void test_is_valid_number();
void test_find_game();
```

### Интеграционные тесты
```bash
./test_create_game.sh
./test_join_game.sh
./test_make_guess.sh
./test_concurrent_clients.sh
```

---

## Лицензия

MIT License - свободное использование в образовательных целях.
