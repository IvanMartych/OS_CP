# Быстрый старт

## За 3 минуты до первой игры! 🚀

### Шаг 1: Установка зависимостей (в WSL)
```bash
sudo apt-get update
sudo apt-get install -y gcc cmake make libzmq3-dev
```

### Шаг 2: Сборка

#### Вариант 1: CMake (рекомендуется)
```bash
cd /mnt/c/MY_DIRECTORY/STUDING_MAI/3_sem/Operating_System/OS_CP
mkdir build
cd build
cmake ..
make
```

#### Вариант 2: Makefile
```bash
cd /mnt/c/MY_DIRECTORY/STUDING_MAI/3_sem/Operating_System/OS_CP
make
```

### Шаг 3: Запуск

**Терминал 1 (WSL):**
```bash
# Если собрали через CMake
cd build
./server

# Если собрали через Makefile
./server
```

**Терминал 2 (WSL):**
```bash
# Если собрали через CMake
cd build
./client

# Если собрали через Makefile
./client
```

**Терминал 3 (WSL) - опционально для второго игрока:**
```bash
cd build  # или корневая папка если Makefile
./client
```

### Шаг 4: Играем!

В клиенте:
1. Введите свое имя
2. Выберите `1` - Создать игру
3. Введите название игры (например: `mygame`)
4. Введите количество игроков (например: `1`)
5. Начинайте угадывать 4-значное число!

---

## Коротко о правилах

Угадайте 4-значное число с уникальными цифрами (0-9):
- **БЫК** = цифра на правильной позиции
- **КОРОВА** = цифра есть, но не на том месте

**Пример:**
```
Секрет: 1234
Ввод: 1567 → 1 БЫК (цифра 1), 0 КОРОВ
Ввод: 1234 → 4 БЫКА (ПОБЕДА!)
```

---

## Проблемы?

### Ошибка: `zmq.h: No such file or directory`
```bash
sudo apt-get install libzmq3-dev
```

### Ошибка: `Cannot bind socket`
Порт 5555 занят. Остановите старый сервер:
```bash
pkill -f "./server"
```

### Сервер не отвечает
Проверьте, что сервер запущен:
```bash
ps aux | grep server
```

---

## Полезные команды

### CMake
```bash
mkdir build && cd build  # Создать папку сборки
cmake ..                 # Настроить проект
make                     # Собрать
./server                 # Запустить сервер
./client                 # Запустить клиент
```

### Makefile
```bash
make              # Собрать проект
make clean        # Очистить
./server          # Запустить сервер
./client          # Запустить клиент
make help         # Показать все команды
```

---

## Что дальше?

- 📖 Читайте [README.md](README.md) для полной документации
- 📚 Смотрите [EXAMPLES.md](EXAMPLES.md) для примеров использования
- 📊 Изучайте [ANALYSIS.md](ANALYSIS.md) для анализа проекта

---

**Удачи! 🎮**
