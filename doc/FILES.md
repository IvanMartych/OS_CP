# 📁 Описание файлов проекта

## 🎯 Исходные файлы (C)

| Файл | Описание | Строк кода |
|------|----------|------------|
| `common.h` | Общие структуры, константы и прототипы функций | ~80 |
| `common.c` | Реализация общих функций (генерация секрета, подсчёт быков/коров) | ~60 |
| `server.c` | Серверная часть - управление играми, обработка запросов | ~330 |
| `client.c` | Клиентская часть - интерфейс пользователя, игровой процесс | ~290 |

**Итого:** ~760 строк чистого кода на C

---

## 🔧 Система сборки

| Файл | Описание |
|------|----------|
| `CMakeLists.txt` | Конфигурация CMake - основная система сборки (рекомендуется) |
| `Makefile` | Традиционный Makefile - альтернативный способ сборки |
| `build.sh` | Автоматический скрипт сборки с проверками зависимостей |

---

## 📚 Документация

| Файл | Назначение | Для кого |
|------|-----------|----------|
| `README.md` | Главная документация проекта | Все |
| `BUILD.md` | Детальная инструкция по сборке | Разработчики |
| `DEFENSE_CHECKLIST.md` | Чеклист и вопросы к защите | Студент |
| `doc/QUICKSTART.md` | Быстрый старт за 3 минуты | Новички |
| `doc/EXAMPLES.md` | Примеры использования и сценарии | Пользователи |
| `doc/ANALYSIS.md` | Технический анализ проекта | Преподаватели |
| `doc/PROJECT_STRUCTURE.md` | Подробная архитектура | Разработчики |

---

## 🛠️ Служебные файлы

| Файл | Описание |
|------|----------|
| `.gitignore` | Список игнорируемых файлов для Git |
| `run.sh` | Скрипт для удобного запуска сервера/клиента (в doc/) |

---

## 📊 Статистика проекта

### Размер кодовой базы
- **Исходный код C:** ~760 строк
- **Документация:** ~2500 строк
- **Всего:** ~3260 строк

### Файловая структура
```
OS_CP/
├── 📄 Исходники (4 файла)
│   ├── common.h
│   ├── common.c
│   ├── server.c
│   └── client.c
│
├── 🔧 Сборка (3 файла)
│   ├── CMakeLists.txt    ⭐ Основной
│   ├── Makefile
│   └── build.sh
│
├── 📚 Документация (8 файлов)
│   ├── README.md         ⭐ Главный
│   ├── BUILD.md
│   ├── DEFENSE_CHECKLIST.md
│   └── doc/
│       ├── QUICKSTART.md
│       ├── EXAMPLES.md
│       ├── ANALYSIS.md
│       ├── PROJECT_STRUCTURE.md
│       └── run.sh
│
└── 🛠️ Служебное (1 файл)
    └── .gitignore
```

---

## 🎯 Ключевые компоненты

### 1. Протокол связи (common.h)
```c
typedef enum {
    MSG_CREATE_GAME,    // Создать игру
    MSG_JOIN_GAME,      // Присоединиться
    MSG_FIND_GAME,      // Автопоиск
    MSG_MAKE_GUESS,     // Попытка угадать
    // ...
} MessageType;

typedef struct {
    MessageType type;
    char game_name[64];
    char player_name[32];
    int guess[4];
    // ...
} Message;
```

### 2. Игровая логика (common.c)
```c
void generate_secret(int *secret);           // Генерация секрета
void calculate_bulls_cows(...);              // Подсчёт быков/коров
int is_valid_number(int *number);            // Валидация
```

### 3. Сервер (server.c)
```c
typedef struct {
    char name[64];
    int secret[4];
    Player players[10];
    int is_finished;
    char winner[32];
} Game;

void handle_create_game(...);
void handle_join_game(...);
void handle_make_guess(...);
```

### 4. Клиент (client.c)
```c
void print_menu();
void create_game(...);
void join_game(...);
void find_game(...);
void play_game(...);
```

---

## 🚀 Использование

### Сборка с CMake (рекомендуется)
```bash
mkdir build && cd build
cmake ..
make
```

### Сборка с Makefile
```bash
make
```

### Автоматическая сборка
```bash
chmod +x build.sh
./build.sh
```

### Запуск
```bash
# CMake
cd build
./server &
./client

# Makefile
./server &
./client
```

---

## 📦 Зависимости

### Обязательные
- `gcc` - компилятор C
- `cmake` >= 3.10 - система сборки
- `make` - утилита сборки
- `libzmq3-dev` - библиотека ZeroMQ
- `pkg-config` - поиск библиотек

### Установка (Ubuntu/Debian)
```bash
sudo apt-get install -y gcc cmake make libzmq3-dev pkg-config
```

---

## 🎓 Соответствие требованиям

✅ Консольная клиент-серверная игра  
✅ Использование очередей сообщений (ZeroMQ)  
✅ Игра "Быки и коровы"  
✅ Две программы: server и client  
✅ Создание игры с именем  
✅ Присоединение к игре  
✅ Указание количества игроков  
✅ Поддержка нескольких игроков  
✅ Функция автопоиска игры  
✅ Сборка через CMake  

---

## 📈 Метрики качества

| Метрика | Значение |
|---------|----------|
| Покрытие требований | 100% |
| Документация | Полная |
| Читаемость кода | Высокая |
| Комментарии | Присутствуют |
| Тестирование | Ручное |
| Ошибки компиляции | 0 |
| Предупреждения | 0 |

---

## 🏆 Преимущества проекта

1. **Полная документация** - 8 файлов документации
2. **Две системы сборки** - CMake (современная) + Makefile (классическая)
3. **Автоматизация** - скрипты build.sh и run.sh
4. **Чистый код** - следование стандартам C11
5. **Расширяемость** - модульная архитектура
6. **Примеры использования** - детальные сценарии
7. **Чеклист для защиты** - готовые ответы на вопросы

---

## 📝 Лицензия

MIT License - свободное использование в образовательных целях

---

**Проект готов к сдаче и защите! ✅**
