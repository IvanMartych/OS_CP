# Инструкция по сборке проекта

## Система сборки

Проект поддерживает две системы сборки:
- **CMake** (рекомендуется) - современная кроссплатформенная система
- **Makefile** (альтернатива) - классический подход

## Быстрый старт с CMake

### 1. Установка зависимостей
```bash
sudo apt-get update
sudo apt-get install -y gcc cmake make libzmq3-dev
```

### 2. Сборка
```bash
# Создать директорию для сборки
mkdir build
cd build

# Настроить проект
cmake ..

# Собрать
make
```

### 3. Запуск
```bash
# В папке build/
./server    # Запустить сервер
./client    # Запустить клиент (в другом терминале)
```

### 4. Очистка
```bash
# Удалить всю папку сборки
cd ..
rm -rf build
```

---

## Альтернатива: Makefile

### Сборка
```bash
make
```

### Запуск
```bash
./server    # Запустить сервер
./client    # Запустить клиент
```

### Очистка
```bash
make clean
```

---

## Детали CMakeLists.txt

### Минимальная версия CMake
```cmake
cmake_minimum_required(VERSION 3.10)
```

### Поиск зависимостей
```cmake
find_package(PkgConfig REQUIRED)
pkg_check_modules(ZMQ REQUIRED libzmq)
```

### Сборка исполняемых файлов
```cmake
# Сервер
add_executable(server server.c common.c)
target_link_libraries(server ${ZMQ_LIBRARIES})

# Клиент
add_executable(client client.c common.c)
target_link_libraries(client ${ZMQ_LIBRARIES})
```

---

## Преимущества CMake

✅ **Кроссплатформенность** - работает на Linux, macOS, Windows  
✅ **Автоматический поиск библиотек** - находит ZeroMQ автоматически  
✅ **Out-of-source сборка** - не засоряет исходники  
✅ **Современный стандарт** - используется в большинстве проектов  
✅ **IDE интеграция** - поддержка CLion, VS Code и др.

---

## Проверка установки

### Проверка CMake
```bash
cmake --version
# Должно быть >= 3.10
```

### Проверка ZeroMQ
```bash
pkg-config --modversion libzmq
# Должна вернуть версию (например, 4.3.4)
```

### Проверка сборки
```bash
cd build
./server --help 2>&1 | head -n 1
# Если запускается - всё работает
```

---

## Устранение проблем

### Ошибка: CMake не найден
```bash
sudo apt-get install cmake
```

### Ошибка: libzmq не найден
```bash
sudo apt-get install libzmq3-dev pkg-config
```

### Ошибка: Не найден pkg-config
```bash
sudo apt-get install pkg-config
```

### Полная переустановка зависимостей
```bash
sudo apt-get update
sudo apt-get install -y gcc g++ cmake make libzmq3-dev pkg-config
```

---

## Использование в других ОС

### macOS (с Homebrew)
```bash
brew install cmake zeromq pkg-config
mkdir build && cd build
cmake ..
make
```

### Windows (с MinGW)
```bash
# Установить MSYS2/MinGW
pacman -S mingw-w64-x86_64-cmake mingw-w64-x86_64-zeromq
mkdir build && cd build
cmake -G "MinGW Makefiles" ..
mingw32-make
```

---

## Дополнительные опции CMake

### Debug сборка
```bash
cmake -DCMAKE_BUILD_TYPE=Debug ..
make
```

### Release сборка (оптимизация)
```bash
cmake -DCMAKE_BUILD_TYPE=Release ..
make
```

### Установка в систему
```bash
sudo make install
# Установит в /usr/local/bin/
```

### Параллельная сборка
```bash
make -j4  # Использовать 4 ядра
```

---

## Рекомендации

1. **Для разработки**: используйте Debug сборку
2. **Для сдачи проекта**: используйте Release сборку
3. **Для тестирования**: используйте out-of-source сборку (build/)
4. **Для чистоты**: всегда используйте отдельную папку build/

---

**Удачной сборки! 🚀**
